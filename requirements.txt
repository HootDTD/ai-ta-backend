fastapi
python-multipart
pymupdf
uvicorn[standard]
gunicorn
pydantic>=2
SQLAlchemy[asyncio]>=2
pgvector
asyncpg
weasyprint>=61
Markdown>=3.6
pygments>=2.18
openai
numpy
tiktoken
python-dotenv
requests
sympy>=1.12
aiosqlite
pytest-asyncio
neo4j>=5.27,<6
rapidfuzz>=3.12,<4
# Apollo NLI resolver tier — now DEFAULT-ON (APOLLO_NLI_ENABLED default true).
# The CPU-only torch index keeps the image slim: NLI runs on CPU (NLI_DEVICE=cpu),
# so the ~2.7GB CUDA wheel is unnecessary. pip prefers the "+cpu" local version
# from this extra index over the plain PyPI wheel. (Windows local dev: if the
# newest +cpu wheel fails to load — WinError 1114 — pin torch==2.6.0; Linux is
# unaffected. See requirements-nli.txt for the historical note.)
--extra-index-url https://download.pytorch.org/whl/cpu
transformers>=4.44,<5
torch>=2.2
sentencepiece
