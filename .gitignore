__pycache__/
*.py[cod]
.venv*/
tmp_uploads/
ai_ta.db
runtime/
artifacts/
.env
.env.*
!.env.example
.DS_Store

# Logs
*.log
uvicorn.log

# Test / coverage artifacts
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Generated knowledge-store artifacts (pgvector is authoritative; km_* dirs,
# FAISS/SQLite/embedding outputs, and source PDFs are rebuildable or belong in
# Supabase Storage — never git)
subjects/

# Sample PDF inputs for the layout embedder CLI (large binaries, not code)
text-embeder/*.pdf

# Locally installed agent skills (personal tooling)
.agents/
.claude/skills/
skills-lock.json

# Feller workflow task artifacts (transient scratchpads / verification reports)
.feller/

# Apollo local-run helpers + transient artifacts (machine-specific paths; not for commit)
apollo_run_env.sh
apollo_run_launch.py
bundle.json
parsed_task.json
auth.users
RUNBOOK.md

# SDD / Superpowers scratch artifacts (transient scratchpads)
.superpowers/

# Local HF model cache for the campaign harness's NLI pre-warm step
# (campaign/infra/prewarm_nli.py) — large binary checkpoint files, never git.
.hf-cache/
