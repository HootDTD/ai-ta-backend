{
  "id": "trapezoidal_x_squared_n4",
  "concept_id": "numerical_integration",
  "difficulty": "intro",
  "given_values": {
    "a": 0.0,
    "b": 2.0,
    "n": 4.0
  },
  "problem_text": "Use the Trapezoidal Rule with n = 4 subintervals to approximate integral_0^2 x^2 dx. Report the estimate.",
  "layer1_seeded": true,
  "declared_paths": [
    [
      "step_size",
      "trapezoidal_rule",
      "endpoint_interior_weights",
      "plan_compute_step_size",
      "plan_evaluate_nodes",
      "plan_apply_trapezoidal"
    ]
  ],
  "target_unknown": "T",
  "symbolic_mappings": {
    "Delta x": "h",
    "T_4": "T"
  },
  "reference_solution": [
    {
      "id": "step_size",
      "step": 1,
      "content": {
        "label": "Step size Delta x = (b - a)/n",
        "symbolic": "h - (b - a)/n",
        "variables": [
          "h",
          "b",
          "a",
          "n"
        ]
      },
      "depends_on": [],
      "entity_key": "eq.step_size",
      "entry_type": "equation"
    },
    {
      "id": "trapezoidal_rule",
      "step": 2,
      "content": {
        "label": "Trapezoidal Rule with n=4: T = (Delta x/2)(f0 + 2 f1 + 2 f2 + 2 f3 + f4)",
        "symbolic": "T - (h/2)*(f0 + 2*f1 + 2*f2 + 2*f3 + f4)",
        "variables": [
          "T",
          "h",
          "f0",
          "f1",
          "f2",
          "f3",
          "f4"
        ]
      },
      "depends_on": [],
      "entity_key": "eq.trapezoidal_rule",
      "entry_type": "equation"
    },
    {
      "id": "endpoint_interior_weights",
      "step": 3,
      "content": {
        "concept": "Trapezoidal weighting pattern",
        "meaning": "The two endpoint nodes f0 and fn carry weight 1; every interior node carries weight 2."
      },
      "depends_on": [],
      "entity_key": "def.endpoint_interior_weights",
      "entry_type": "definition"
    },
    {
      "id": "plan_compute_step_size",
      "step": 4,
      "content": {
        "label": "compute the step size Delta x = (2 - 0)/4 = 0.5",
        "order": 1,
        "action": "compute the step size Delta x = (2 - 0)/4 = 0.5",
        "purpose": "fix the 5 equally spaced nodes 0, 0.5, 1, 1.5, 2",
        "uses_equations": [
          "step_size"
        ]
      },
      "depends_on": [],
      "entity_key": "proc.plan_compute_step_size",
      "entry_type": "procedure_step"
    },
    {
      "id": "plan_evaluate_nodes",
      "step": 5,
      "content": {
        "label": "evaluate f(x) = x^2 at each node: f0=0, f1=0.25, f2=1, f3=2.25, f4=4",
        "order": 2,
        "action": "evaluate f(x) = x^2 at each node: f0=0, f1=0.25, f2=1, f3=2.25, f4=4",
        "purpose": "obtain the node values that feed the weighted sum",
        "uses_equations": []
      },
      "depends_on": [
        "plan_compute_step_size",
        "endpoint_interior_weights"
      ],
      "entity_key": "proc.plan_evaluate_nodes",
      "entry_type": "procedure_step"
    },
    {
      "id": "plan_apply_trapezoidal",
      "step": 6,
      "content": {
        "label": "substitute the node values into T = (0.5/2)(0 + 2(0.25) + 2(1) + 2(2.25) + 4) and compute T_4 = 2.75",
        "order": 3,
        "action": "substitute the node values into T = (0.5/2)(0 + 2(0.25) + 2(1) + 2(2.25) + 4) and compute T_4 = 2.75",
        "purpose": "produce the trapezoidal estimate of the integral",
        "uses_equations": [
          "trapezoidal_rule"
        ]
      },
      "depends_on": [
        "plan_evaluate_nodes"
      ],
      "entity_key": "proc.plan_apply_trapezoidal",
      "entry_type": "procedure_step"
    }
  ]
}
